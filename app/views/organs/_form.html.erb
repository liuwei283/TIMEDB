<%= form_for @organ do |f| %>
 
  <% if @organ.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@organ.errors.count, "error") %> prohibited
        this organ from being saved:
      </h2>
      <ul>
        <% @organ.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% @attrs.each do |attr| %>
    <% if attr != 'id' && attr != 'created_at' && attr != 'updated_at' %>
      <% if attr == 'cover_image' %>
        <p> 
          <%= f.label attr.capitalize %> <br>
          <%= f.text_field :cover_image %>
        </p>
        <% if !@organ.cover_image.nil? %>
          <%#= image_tag(@organ.cover_image, width: '400') %>
        <% end %>
      <% else %>
        <p> 
          <%= f.label attr.capitalize %> <br>
          <%= f.text_field attr %>
        </p>
      <% end %>
    <% end %>
  <% end %>
  <p>
    <%= f.submit %>
  </p>
<% end %>
