svg {
	width = 1200
	height = 600
	Component {
		ComplexStackedBar {
			x = 50
			y = 50
			baseWidth = Math.min(1100/Object.keys(data.widMap).length, 40)
			plotSize = [1100, 500]
			@expr console.log(data)
			@props data
		}
		// Legend {
		// 	x = @geo(100, -40); y = 40
		// 	anchor = @anchor("top", "right")
		// 	data = data.colorMap.legendData()
		// 	@expr console.log(data.colorMap.legendData())
		// }
	}
}